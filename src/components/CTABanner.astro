---
interface Props {
  title: string;
  ctaText: string;
  ctaHref?: string;
}
const { title, ctaText, ctaHref = '/kontakt' } = Astro.props;
---

<section class="bg-brand text-white">
  <div class="container-custom py-16 md:py-20">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold mb-6 text-balance">
        {title}
      </h2>
      <button 
        data-consultation
        class="inline-block px-8 py-4 bg-accent text-white font-semibold rounded-lg hover:bg-brand transition-all transform hover:scale-105 cursor-pointer"
      >
        {ctaText}
      </button>
    </div>
  </div>
</section>

<style>
  :global([data-consultation]:hover) {
    background-color: #111827 !important;
  }
</style>

